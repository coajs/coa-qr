{
  "name": "coa-qr",
  "version": "1.0.1",
  "description": "qr component for coa",
  "keywords": [
    "coajs",
    "coa",
    "qr"
  ],
  "license": "MIT",
  "author": "Aex",
  "homepage": "https://github.com/coajs/coa-qr",
  "repository": {
    "type": "git",
    "url": "https://github.com/coajs/coa-qr.git"
  },
  "scripts": {
    "dev": "tsc -w",
    "build": "rm -rf dist && tsc && cp package.json dist",
    "npm-publish": "yarn build && yarn version --patch && cp package.json README.md dist && cd dist && npm publish"
  },
  "dependencies": {
    "qrcode": "^1.4.4"
  },
  "devDependencies": {
    "@types/node": "^14.0.5",
    "@types/qrcode": "^1.3.4",
    "typescript": "^3.9.3"
  }
}
